import { Link } from 'react-router-dom'
export default function PlaceCard({place,selected,onToggle,compact=false}){return <article className={`group relative overflow-hidden rounded-2xl text-white shadow-float ${compact?'min-h-[260px]':'min-h-[350px]'}`}><div className="place-photo absolute inset-0 transition duration-700 group-hover:scale-105" style={{backgroundPosition:place.position}}></div><div className={`absolute inset-0 bg-gradient-to-t ${place.accent}`}></div><div className="relative flex h-full min-h-[inherit] flex-col justify-between p-6"><div className="flex justify-between"><span className="rounded-full border border-white/30 bg-black/10 px-3 py-1 font-mono text-[10px] tracking-wider">{place.kind.toUpperCase()}</span>{onToggle&&<button onClick={()=>onToggle(place.slug)} className={`grid h-9 w-9 place-items-center rounded-full transition ${selected?'bg-saffron text-ink':'bg-white/15 hover:bg-white hover:text-ink'}`} aria-label={`Save ${place.name}`}>{selected?'✓':'+'}</button>}</div><div><p className="font-mono text-xs text-saffron">{place.best} · {place.duration}</p><h3 className="mt-2 font-display text-4xl leading-none">{place.name}</h3><p className="mt-3 max-w-xs text-sm leading-5 text-white/85">{place.description}</p><Link to="/places" className="mt-4 inline-block border-b border-white/40 pb-1 text-xs">See travel details ↗</Link></div></div></article>}
